# tempo.yaml
server:
  http_listen_address: "0.0.0.0"
  http_listen_port: ${PORT} # <--- L'expansion d'env var va remplacer ceci
  log_level: info

distributor:
  receivers:
    otlp:
      protocols:
        http:
          endpoint: "0.0.0.0:${PORT}" # <--- L'expansion d'env var va remplacer ceci
        grpc:
          endpoint: "0.0.0.0:4317"
# Configuration du stockage (backend 'local' pour le stockage sur le disque du conteneur)
storage:
  trace:
    backend: local
    # Utiliser /tmp/ pour l'écriture (obligatoire sur les plateformes PaaS)
    wal:
      path: /tmp/tempo-data/wal 
    local:
      path: /tmp/tempo-data/blocks 

# Configuration du compresseur
compactor:
  compaction:
    block_retention: 24h
    compacted_block_retention: 1h

# --- SECTION SUPPRIMÉE POUR LA CORRECTION ---
# La section 'limits' causait une erreur d'analyse dans la configuration de Tempo 2.x.
# Vous pourrez la réintroduire plus tard sous la forme correcte (ex: overrides: max_bytes_per_trace: 1000000)