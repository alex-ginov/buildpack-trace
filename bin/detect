#!/bin/bash

# Vérifie si c'est une application Tempo
if [ -f "$1/config/tempo.yaml" ] || [ -f "$1/tempo.yaml" ] || [ -f "$1/tempo.yml" ]; then
  echo "Tempo Application"
  exit 0
else
  echo "no" >&2
  exit 1
fi