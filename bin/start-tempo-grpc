#!/bin/bash
set -e

echo "===== TCP GATEWAY DUMMY PROCESS ====="
echo "This process is only here to expose gRPC port ${TCP_GRPC_PORT:-4317} via Scalingo's TCP Gateway."
echo "Tempo is running in the 'web' process."

# Boucle infinie portable
while true; do
    sleep 3600
done