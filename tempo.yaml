server:
  http_listen_port: 3200
  http_listen_address: "127.0.0.1"

distributor:
  receivers:
    otlp:
      protocols:
        http:
          endpoint: "127.0.0.1:4318"
        grpc:
          endpoint: "127.0.0.1:4317"
    jaeger:
      protocols:
        thrift_http:
          endpoint: "127.0.0.1:14268"
    zipkin:
      endpoint: "127.0.0.1:9411"

storage:
  trace:
    backend: s3
    s3:
      endpoint: "192.168.194.77:9001"  # juste l'hôte et port MinIO
      bucket: "poc"
      access_key: "minioadmin"
      secret_key: "minioadmin"
      insecure: true
      forcepathstyle: true


compactor:
  compaction:
    compacted_block_retention: 48h
